# v0.1.3

Released 2026-08-04.

## Included

- Restores the claim-first Compact/Full viewer as the packaged viewer source.
- Adds the Claim browser, adjacent node inspector, folded provenance trees,
  deterministic evidence-flow layout, local drag behavior, and Result-only
  aggregation.
- Keeps the mandatory CLI preflight and final validation gate in the extraction
  skill.
- Adds measurement and quote-normalization fixes plus regression coverage.
- Adds viewer and manifest regression checks that prevent an artifact/source
  mismatch from silently returning.

## Verification

- Full suite: 59 passed.
- Real Luna-generated `paper-evidence-graph/0.2` build: valid, with packaged
  viewer asset hashes matching source.
- Standalone `.pyz` build outside the repository: passed.
- Clean virtual-environment wheel install and build: passed.
- GitHub Actions build, GitHub Release, and PyPI trusted publishing: passed.

Release: <https://github.com/ZakuZakuu/papergraph/releases/tag/v0.1.3>
