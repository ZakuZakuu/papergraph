# README assets

Place the repository screenshot at `demo-screenshot.png`.

Both top-level READMEs intentionally refer to that exact path. Use a wide PNG that shows the Claim browser and enough of the graph to make the workflow legible at a glance.
